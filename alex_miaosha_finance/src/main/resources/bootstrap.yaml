spring:
  cloud:
    nacos:
      server-addr: ${nacos.url}
      config:
        server-addr: ${nacos.url}
        file-extension: yaml
        group: ${nacos.group}
        namespace: ${nacos.namaspace}
        extension-configs:
          - data-id: redis.yaml
            group: ${nacos.group}
            refresh: true
          - data-id: nacos-server.yaml
            group: ${nacos.group}
            refresh: true
          - data-id: knife4j.yaml
            group: ${nacos.group}
            refresh: true
          - data-id: springbootadmin.yaml
            group: ${nacos.group}
            refresh: true

nacos:
  url: localhost:8848
  group: alex-miaosha
  namaspace: 033377eb-973b-4dac-a0e9-e99c87325009