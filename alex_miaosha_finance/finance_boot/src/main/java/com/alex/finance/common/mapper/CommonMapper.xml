<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.alex.finance.common.mapper.CommonMapper">
    <!-- 
        description: 根据用户ID查询用户所在公司下的所有用户ID
        author: alex
        createDate: 2025-01-XX
        AI Agent
    -->
    <sql id="familyGroup">
        SELECT
            DISTINCT oui2.user_id AS id
        FROM
            alex_user.t_org_user_info oui1
                INNER JOIN alex_user.t_org_user_info oui2 ON
                oui1.org_id = oui2.org_id
        WHERE
            oui1.user_id = #{userId}
          AND oui1.is_delete = 0
          AND oui1.status = '1'
          AND oui2.is_delete = 0
          AND oui2.status = '1'
    </sql>
</mapper>