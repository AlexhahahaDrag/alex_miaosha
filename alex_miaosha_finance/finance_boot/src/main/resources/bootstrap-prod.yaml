spring:
  cloud:
    nacos:
      server-addr: ${nacos.url}
      username: ${nacos.username}
      password: ${nacos.password}
      config:
        server-addr: ${nacos.url}
        file-extension: ${nacos.file-extension}
        group: ${nacos.group}
        namespace: ${nacos.namaspace}
        extension-configs:
          - data-id: ${nacos.redis}.${nacos.file-extension}
            group: ${nacos.group}
            refresh: true
          - data-id: ${nacos.nacos}.${nacos.file-extension}
            group: ${nacos.group}
            refresh: true
          - data-id: ${nacos.knife4j}.${nacos.file-extension}
            group: ${nacos.group}
            refresh: true
          - data-id: ${nacos.admin}.${nacos.file-extension}
            group: ${nacos.group}
            refresh: true
          - data-id: ${nacos.jasypt}.${nacos.file-extension}
            group: ${nacos.group}
            refresh: true
          - data-id: ${nacos.audience}.${nacos.file-extension}
            group: ${nacos.group}
            refresh: true
          - data-id: ${nacos.wechat}.${nacos.file-extension}
            group: ${nacos.group}
            refresh: true

nacos:
  url: nacos:8848
  group: alex-miaosha-prod
  namaspace: 439dbcdc-b721-4afd-abee-08184739eb75
  username: nacos
  password: nacos
  file-extension: yaml
  knife4j: knife4j
  admin: springbootadmin
  nacos: nacos-service
  redis: redis
  jasypt: jasypt-user
  audience: audience
  wechat: wechat