server:
  port: 30008

spring:
  application:
    name: alex-finance-${spring.profiles.active}
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource            # 当前数据源操作类型
    driver-class-name: com.mysql.cj.jdbc.Driver             # mysql驱动包
    url: jdbc:mysql://mysql:3306/alex_finance?useUnicode=true&characterEncoding=utf-8&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: ENC(wzGvorwuoFra8yDJA66Xfg==)
    password: ENC(TQ2oVKN42O4FWPbyKH7mCHBwhNc4xNhLZa2IBDN93TI=)

mybatis-plus:
  # AI Agent: 修复 MyBatis mapper-locations 配置
  # 原问题：XML 文件在 src/main/java 目录下，但配置只从 classpath 查找，导致找不到 SQL 语句
  # 解决：配置支持从 classpath 和 java 目录下的编译输出中查找
  mapper-locations:
    - classpath*:com/alex/finance/**/mapper/*Mapper.xml

xxl:
  job:
    executor:
      appname: xxl-finance-prod
      logretentiondays: 30
      port: 8008
      accessToken: asdfghjkl