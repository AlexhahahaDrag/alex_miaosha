package com.baomidou.mybatisplus.generator.config;

public enum OutputFile {
    entity,
    service,
    serviceImpl,
    mapper,
    mapperXml,
    controller,
    vo,
    client,
    other,
    feign;

    private OutputFile() {
    }
}
